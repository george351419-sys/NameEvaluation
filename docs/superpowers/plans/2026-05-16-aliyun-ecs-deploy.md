# 阿里云 ECS 部署包 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 `/Users/bessie/cursor/name/name-deploy/` 创建一个完整的部署包，包含 MySQL 版源码、一键部署脚本和说明文档，可直接上传到阿里云 ECS。

**Architecture:** 从现有 `app/` 目录复制源码到 `name-deploy/app/`，同时将 Prisma 数据源从 SQLite 切换为 MySQL，并新增 PM2 配置、deploy.sh 脚本和 README。部署包不含 `node_modules`、`.next`、`dev.db`，上传到 ECS 后运行 `deploy.sh` 完成安装和启动。

**Tech Stack:** Next.js 16, Prisma 5 (MySQL), PM2, Bash

---

## 文件清单

| 操作 | 路径 |
|------|------|
| 新建（复制+修改） | `name-deploy/app/prisma/schema.prisma` |
| 新建（覆盖旧迁移） | `name-deploy/app/prisma/migrations/0001_init/migration.sql` |
| 新建（覆盖旧迁移） | `name-deploy/app/prisma/migrations/migration_lock.toml` |
| 新建 | `name-deploy/.env.example` |
| 新建 | `name-deploy/ecosystem.config.js` |
| 新建 | `name-deploy/deploy.sh` |
| 新建 | `name-deploy/README.md` |

---

### Task 1: 复制源码到 name-deploy/

**Files:**
- Create: `name-deploy/app/` (从 `app/` rsync)

- [ ] **Step 1: 用 rsync 复制源码，排除不需要的文件**

```bash
rsync -av \
  --exclude='node_modules' \
  --exclude='.next' \
  --exclude='dev.db' \
  --exclude='.env' \
  --exclude='*.db' \
  --exclude='tsconfig.tsbuildinfo' \
  /Users/bessie/cursor/name/app/ \
  /Users/bessie/cursor/name/name-deploy/app/
```

- [ ] **Step 2: 验证复制结果**

```bash
ls /Users/bessie/cursor/name/name-deploy/app/
```

Expected: 看到 `app/ components/ prisma/ package.json next.config.ts` 等，**不含** `node_modules/` 和 `dev.db`

- [ ] **Step 3: Commit**

```bash
cd /Users/bessie/cursor/name
git add name-deploy/
git commit -m "feat: 初始化 name-deploy 目录，复制源码"
```

---

### Task 2: 修改 Prisma schema 为 MySQL

**Files:**
- Modify: `name-deploy/app/prisma/schema.prisma`

- [ ] **Step 1: 替换 schema.prisma 中的 datasource 块**

将 `name-deploy/app/prisma/schema.prisma` 中的：

```prisma
datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}
```

改为：

```prisma
datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}
```

- [ ] **Step 2: 验证**

```bash
grep -A 3 "datasource db" /Users/bessie/cursor/name/name-deploy/app/prisma/schema.prisma
```

Expected:
```
datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}
```

- [ ] **Step 3: Commit**

```bash
cd /Users/bessie/cursor/name
git add name-deploy/app/prisma/schema.prisma
git commit -m "feat: Prisma schema 切换为 MySQL"
```

---

### Task 3: 替换 Prisma 迁移文件为 MySQL 版本

**Files:**
- Delete: `name-deploy/app/prisma/migrations/` (旧 SQLite 迁移)
- Create: `name-deploy/app/prisma/migrations/0001_init/migration.sql`
- Create: `name-deploy/app/prisma/migrations/migration_lock.toml`

- [ ] **Step 1: 删除旧的 SQLite 迁移**

```bash
rm -rf /Users/bessie/cursor/name/name-deploy/app/prisma/migrations
mkdir -p /Users/bessie/cursor/name/name-deploy/app/prisma/migrations/0001_init
```

- [ ] **Step 2: 创建 migration_lock.toml**

创建文件 `name-deploy/app/prisma/migrations/migration_lock.toml`，内容：

```toml
# Please do not edit this file manually
# It should be added in your version-control system (e.g., Git)
provider = "mysql"
```

- [ ] **Step 3: 创建 MySQL 建表 SQL**

创建文件 `name-deploy/app/prisma/migrations/0001_init/migration.sql`，内容：

```sql
-- CreateTable
CREATE TABLE `Evaluation` (
    `id` VARCHAR(191) NOT NULL,
    `name` VARCHAR(191) NOT NULL,
    `surname` VARCHAR(191) NOT NULL,
    `givenName` VARCHAR(191) NOT NULL,
    `nameFirst` VARCHAR(191) NOT NULL,
    `nameLast` VARCHAR(191) NOT NULL,
    `birthDate` VARCHAR(191) NOT NULL,
    `isLunar` BOOLEAN NOT NULL DEFAULT false,
    `zodiacOverride` VARCHAR(191) NULL,
    `fatherSurname` VARCHAR(191) NULL,
    `fatherZodiac` VARCHAR(191) NULL,
    `motherSurname` VARCHAR(191) NULL,
    `motherZodiac` VARCHAR(191) NULL,
    `childZodiac` VARCHAR(191) NULL,
    `resultJson` LONGTEXT NOT NULL,
    `interpretation` LONGTEXT NULL,
    `createdAt` DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
    `updatedAt` DATETIME(3) NOT NULL,

    PRIMARY KEY (`id`)
) DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

-- CreateTable
CREATE TABLE `CompanyEvaluation` (
    `id` VARCHAR(191) NOT NULL,
    `companyName` VARCHAR(191) NOT NULL,
    `founderName` VARCHAR(191) NOT NULL,
    `founderZodiac` VARCHAR(191) NULL,
    `partnerNames` VARCHAR(191) NOT NULL,
    `resultJson` LONGTEXT NOT NULL,
    `interpretation` LONGTEXT NULL,
    `createdAt` DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
    `updatedAt` DATETIME(3) NOT NULL,

    PRIMARY KEY (`id`)
) DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

-- CreateTable
CREATE TABLE `NamingEvaluation` (
    `id` VARCHAR(191) NOT NULL,
    `surname` VARCHAR(191) NOT NULL,
    `ownZodiac` VARCHAR(191) NOT NULL,
    `fatherSurname` VARCHAR(191) NOT NULL,
    `fatherZodiac` VARCHAR(191) NULL,
    `motherSurname` VARCHAR(191) NOT NULL,
    `motherZodiac` VARCHAR(191) NOT NULL,
    `resultJson` LONGTEXT NOT NULL,
    `createdAt` DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
    `updatedAt` DATETIME(3) NOT NULL,

    PRIMARY KEY (`id`)
) DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

- [ ] **Step 4: 验证迁移文件结构**

```bash
find /Users/bessie/cursor/name/name-deploy/app/prisma/migrations -type f
```

Expected:
```
.../migrations/migration_lock.toml
.../migrations/0001_init/migration.sql
```

- [ ] **Step 5: Commit**

```bash
cd /Users/bessie/cursor/name
git add name-deploy/app/prisma/migrations/
git commit -m "feat: 添加 MySQL 版 Prisma 迁移文件"
```

---

### Task 4: 创建 .env.example

**Files:**
- Create: `name-deploy/.env.example`

- [ ] **Step 1: 创建 .env.example 文件**

创建文件 `name-deploy/.env.example`，内容：

```env
# =============================================
# 名字评测网站 - 环境变量配置
# 使用方法：cp .env.example app/.env 然后填入真实值
# =============================================

# MySQL 数据库连接（本机安装）
# 格式：mysql://用户名:密码@主机:端口/数据库名
DATABASE_URL="mysql://root:你的MySQL密码@localhost:3306/name_db"

# AI 接口 Key（选填其一或两个都填）
DEEPSEEK_API_KEY="sk-xxx"
MINIMAX_API_KEY="sk-xxx"

# 切换 AI 模型：deepseek 或 minimax
LLM_PROVIDER="deepseek"
```

- [ ] **Step 2: 验证文件存在**

```bash
cat /Users/bessie/cursor/name/name-deploy/.env.example
```

Expected: 看到完整的环境变量模板内容

- [ ] **Step 3: Commit**

```bash
cd /Users/bessie/cursor/name
git add name-deploy/.env.example
git commit -m "feat: 添加环境变量模板 .env.example"
```

---

### Task 5: 创建 PM2 配置文件

**Files:**
- Create: `name-deploy/ecosystem.config.js`

- [ ] **Step 1: 创建 ecosystem.config.js**

创建文件 `name-deploy/ecosystem.config.js`，内容：

```js
module.exports = {
  apps: [
    {
      name: "name-website",
      script: "node_modules/.bin/next",
      args: "start",
      cwd: "./app",
      instances: 1,
      autorestart: true,
      watch: false,
      env: {
        NODE_ENV: "production",
        PORT: 3000,
      },
    },
  ],
};
```

- [ ] **Step 2: 验证文件存在**

```bash
cat /Users/bessie/cursor/name/name-deploy/ecosystem.config.js
```

Expected: 看到 PM2 配置内容，`name` 为 `name-website`，`cwd` 为 `./app`

- [ ] **Step 3: Commit**

```bash
cd /Users/bessie/cursor/name
git add name-deploy/ecosystem.config.js
git commit -m "feat: 添加 PM2 配置文件"
```

---

### Task 6: 创建一键部署脚本 deploy.sh

**Files:**
- Create: `name-deploy/deploy.sh`

- [ ] **Step 1: 创建 deploy.sh**

创建文件 `name-deploy/deploy.sh`，内容：

```bash
#!/bin/bash
set -e

echo "=========================================="
echo "  名字评测网站 - 部署脚本"
echo "=========================================="

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
APP_DIR="$SCRIPT_DIR/app"

# 检查 .env 文件
if [ ! -f "$APP_DIR/.env" ]; then
  echo "❌ 错误：找不到 app/.env 文件"
  echo "   请先执行：cp .env.example app/.env"
  echo "   然后编辑 app/.env 填入真实的数据库密码和 API Key"
  exit 1
fi

echo ""
echo "▶ 安装依赖..."
cd "$APP_DIR"
npm install --production=false

echo ""
echo "▶ 生成 Prisma 客户端..."
npx prisma generate

echo ""
echo "▶ 执行数据库迁移..."
npx prisma migrate deploy

echo ""
echo "▶ 构建 Next.js 生产包..."
npm run build

echo ""
echo "▶ 启动/重启 PM2 服务..."
cd "$SCRIPT_DIR"
if pm2 list | grep -q "name-website"; then
  pm2 restart name-website
else
  pm2 start ecosystem.config.js
fi

pm2 save

echo ""
echo "✅ 部署完成！网站运行在 http://localhost:3000"
echo "   查看日志：pm2 logs name-website"
echo "   查看状态：pm2 status"
```

- [ ] **Step 2: 给脚本添加可执行权限**

```bash
chmod +x /Users/bessie/cursor/name/name-deploy/deploy.sh
```

- [ ] **Step 3: 验证脚本内容**

```bash
head -20 /Users/bessie/cursor/name/name-deploy/deploy.sh
```

Expected: 看到 `#!/bin/bash` 和 set -e 以及部署步骤

- [ ] **Step 4: Commit**

```bash
cd /Users/bessie/cursor/name
git add name-deploy/deploy.sh
git commit -m "feat: 添加一键部署脚本 deploy.sh"
```

---

### Task 7: 创建 README 部署说明

**Files:**
- Create: `name-deploy/README.md`

- [ ] **Step 1: 创建 README.md**

创建文件 `name-deploy/README.md`，内容：

```markdown
# 名字评测网站 - 部署说明

## ECS 前置环境（首次部署时安装，只需一次）

### 1. 安装 Node.js 18+

```bash
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.7/install.sh | bash
source ~/.bashrc
nvm install 18
nvm use 18
```

### 2. 安装 PM2

```bash
npm install -g pm2
pm2 startup
```
按照提示执行输出的命令（用于开机自启）

### 3. 安装 MySQL 8.0

```bash
sudo apt update
sudo apt install -y mysql-server
sudo systemctl start mysql
sudo systemctl enable mysql
```

### 4. 创建数据库和用户

```bash
sudo mysql
```

进入 MySQL 后执行：

```sql
CREATE DATABASE name_db CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
ALTER USER 'root'@'localhost' IDENTIFIED WITH mysql_native_password BY '你的密码';
FLUSH PRIVILEGES;
EXIT;
```

---

## 部署步骤

### 1. 上传文件到 ECS

将整个 `name-deploy/` 文件夹上传到 ECS，例如上传到 `/home/ubuntu/name-deploy/`

```bash
# 本地执行（替换为你的 ECS IP）
scp -r name-deploy/ ubuntu@你的ECS_IP:/home/ubuntu/
```

### 2. 配置环境变量

```bash
cd /home/ubuntu/name-deploy
cp .env.example app/.env
nano app/.env
```

填入真实值：
- `DATABASE_URL`: 替换 `你的MySQL密码` 为实际密码
- `DEEPSEEK_API_KEY`: 填入真实 Key
- `MINIMAX_API_KEY`: 填入真实 Key（如使用 minimax）

### 3. 运行部署脚本

```bash
cd /home/ubuntu/name-deploy
bash deploy.sh
```

脚本会自动完成：安装依赖 → 数据库迁移 → 构建 → 启动

---

## 更新代码

重新上传 `name-deploy/` 后执行：

```bash
cd /home/ubuntu/name-deploy
bash deploy.sh
```

---

## 常用命令

```bash
pm2 status              # 查看运行状态
pm2 logs name-website   # 查看日志
pm2 restart name-website # 手动重启
pm2 stop name-website   # 停止
```

---

## 开放端口

在阿里云控制台 → 安全组，添加入方向规则：
- 端口 3000（直接访问）
- 端口 80（如配置 Nginx 反向代理）
```

- [ ] **Step 2: 验证文件**

```bash
wc -l /Users/bessie/cursor/name/name-deploy/README.md
```

Expected: 行数大于 60

- [ ] **Step 3: Commit**

```bash
cd /Users/bessie/cursor/name
git add name-deploy/README.md
git commit -m "feat: 添加部署说明 README"
```

---

### Task 8: 最终验证部署包完整性

- [ ] **Step 1: 检查目录结构**

```bash
find /Users/bessie/cursor/name/name-deploy -maxdepth 3 -not -path '*/node_modules/*' -not -path '*/.next/*'
```

Expected: 看到以下关键文件：
- `name-deploy/app/prisma/schema.prisma`
- `name-deploy/app/prisma/migrations/0001_init/migration.sql`
- `name-deploy/app/prisma/migrations/migration_lock.toml`
- `name-deploy/.env.example`
- `name-deploy/ecosystem.config.js`
- `name-deploy/deploy.sh`
- `name-deploy/README.md`

- [ ] **Step 2: 确认没有包含敏感文件**

```bash
find /Users/bessie/cursor/name/name-deploy -name ".env" -not -name ".env.example"
```

Expected: 无输出（没有真实的 .env 文件）

```bash
find /Users/bessie/cursor/name/name-deploy -name "*.db"
```

Expected: 无输出（没有数据库文件）

- [ ] **Step 3: 确认 schema 已切换为 MySQL**

```bash
grep "provider" /Users/bessie/cursor/name/name-deploy/app/prisma/schema.prisma
```

Expected:
```
provider = "prisma-client-js"
provider = "mysql"
```

- [ ] **Step 4: 最终 Commit**

```bash
cd /Users/bessie/cursor/name
git add -A
git commit -m "feat: 完成阿里云 ECS 部署包（name-deploy/）"
```
